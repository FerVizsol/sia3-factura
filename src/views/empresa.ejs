<%- include('partials/_header') %>
<h1 class="mt-3 ms-3">EMPRESA</h1>
<div class="container">
    <div class="row mt-5">
        <div class="col-md-7">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>Codigo RUC</th>
                        <th>Nombre</th>
                        <th>E-Mail</th>
                        <th>Direccion</th>
                        <th>Telefono</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (data) { %>
                        <% for (var i = 0; i < data.length; i++) { %>
                            <tr>
                                <td><%= data[i].codEmpresa %></td>
                                <td><%= data[i].nombreEmpresa %></td>
                                <td><%= data[i].correoElectronico %></td>
                                <td><%= data[i].direccion %></td>
                                <td><%= data[i].telefono %></td>
                            </tr>
                        <% } %>
                    <% } %>
                </tbody>
                
            </table>
        </div>
        <div class="col-md-5">
            <div class="card">
                <div class="card-body">
                    <form action="/addEmpresa" method="POST">
                        <div class="form-group mb-2">
                            <input type="number" name="cod" placeholder="Numero RUC" class="form-control mb-2">
                            <input type="text" name="name" placeholder="Nombre completo" class="form-control mb-2">
                            <input type="email" name="mail" placeholder="E-mail" class="form-control mb-2">
                            <input type="text" name="dir" placeholder="Direccion" class="form-control mb-2">
                            <input type="tel" name="phone" placeholder="Telefono" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-primary">
                            Agregar Empresa
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<%- include('partials/_footer') %>